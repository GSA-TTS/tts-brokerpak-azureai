# yaml-language-server: $schema=https://json.schemastore.org/cloudfoundry-application-manifest.json
applications:
  - name: ai-app
    buildpacks:
      - go_buildpack
    services:
      - your-ai-service # fill this in, or delete it and bind to a service after push.
